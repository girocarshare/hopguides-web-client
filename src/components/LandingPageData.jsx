import React, {
    forwardRef,
} from "react";


const LandingPageData = forwardRef((props, ref) => {

    return (

        <div>
            <h1>This is landing page</h1>
        </div>

    );
});

export default LandingPageData
